# load tensorflow ranking package
import tensorflow_ranking as tf_ranking

# load tensorflow
import tensorflow as tf

# load numpy
import numpy as np

# load pandas
import pandas as pd

# load tensorflow iris dataset
from tensorflow.keras.datasets import iris

# load data
(x_train, y_train), (x_test, y_test) = iris.load_data()


